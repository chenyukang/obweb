ob-web:
 build: .
 ports:
  - "8006:8006"
 volumes:
  - ~/.ssh:/root/.ssh
  - ./db:/ob-web-pack/db:rw
  - ./ob:/ob-web-pack/ob:rw
  - ./pages:/ob-web-pack/pages:rw
 command: npm run dev
 working_dir: /ob-web-pack